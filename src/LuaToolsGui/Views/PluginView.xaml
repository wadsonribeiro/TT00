<UserControl
    x:Class="LuaToolsGui.Views.PluginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:res="clr-namespace:LuaToolsGui.Resources"
    xmlns:vm="clr-namespace:LuaToolsGui.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:PluginViewModel}"
    TextElement.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid x:Name="Root">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="760" Margin="8,16,16,24">

                <!-- Header -->
                <TextBlock
                    FontSize="22"
                    FontWeight="Bold"
                    Text="{x:Static res:Strings.Plugin_Title}" />
                <TextBlock
                    Margin="0,2,0,0"
                    FontSize="13"
                    Foreground="#9ca3af"
                    Text="{x:Static res:Strings.Plugin_Subtitle}"
                    TextWrapping="Wrap" />

                <!-- Status card -->
                <Border
                    Margin="0,18,0,0"
                    Padding="18"
                    Background="#0DFFFFFF"
                    BorderBrush="#1FFFFFFF"
                    BorderThickness="1"
                    CornerRadius="10">
                    <StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon
                                VerticalAlignment="Center"
                                Foreground="#38bdf8"
                                Symbol="PuzzlePiece24" />
                            <TextBlock
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                FontSize="15"
                                FontWeight="SemiBold"
                                Text="{x:Static res:Strings.Plugin_CardTitle}" />
                            <Border
                                Margin="10,0,0,0"
                                Padding="7,2"
                                VerticalAlignment="Center"
                                Background="#207c3aed"
                                CornerRadius="4"
                                Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}">
                                <TextBlock
                                    FontSize="10.5"
                                    FontWeight="SemiBold"
                                    Foreground="#c4b5fd"
                                    Text="{x:Static res:Strings.Plugin_Status_Installed}" />
                            </Border>
                            <Border
                                Margin="6,0,0,0"
                                Padding="7,2"
                                VerticalAlignment="Center"
                                Background="#20f59e0b"
                                CornerRadius="4"
                                Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVis}}">
                                <TextBlock
                                    FontSize="10.5"
                                    FontWeight="SemiBold"
                                    Foreground="#fbbf24"
                                    Text="{x:Static res:Strings.Plugin_Badge_UpdateAvailable}" />
                            </Border>
                        </StackPanel>

                        <!-- Status rows: each has a leading colored status glyph so state reads at a glance. -->
                        <Grid Margin="0,16,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="26" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Version -->
                            <ui:SymbolIcon Grid.Row="0" Grid.Column="0" Margin="0,4" VerticalAlignment="Center" FontSize="16" Foreground="#7c8593" Symbol="Tag24" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,4" VerticalAlignment="Center" FontSize="12.5" Foreground="#7c8593" Text="{x:Static res:Strings.Plugin_Row_InstalledVersion}" />
                            <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,4" VerticalAlignment="Center" Orientation="Horizontal">
                                <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#e5e7eb" Text="{Binding InstalledVersion}" />
                                <!-- Update available: arrow to the newer tag, amber. -->
                                <StackPanel Orientation="Horizontal" Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Margin="7,0,7,0" FontSize="12.5" Foreground="#7c8593" Text="→" />
                                    <TextBlock FontSize="12.5" FontWeight="SemiBold" Foreground="#fbbf24" Text="{Binding LatestVersion}" />
                                </StackPanel>
                                <!-- Up to date pill, green. -->
                                <Border Margin="8,0,0,0" Padding="6,1" Background="#2022c55e" CornerRadius="4"
                                        Visibility="{Binding ShowUpToDate, Converter={StaticResource BoolToVis}}">
                                    <TextBlock FontSize="10.5" FontWeight="SemiBold" Foreground="#4ade80" Text="{x:Static res:Strings.Plugin_Status_UpToDate}" />
                                </Border>
                            </StackPanel>

                            <!-- Frontend -->
                            <Grid Grid.Row="1" Grid.Column="0" Margin="0,4" VerticalAlignment="Center">
                                <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding FrontendInstalled, Converter={StaticResource BoolToVis}}" />
                                <ui:SymbolIcon FontSize="16" Foreground="#7c8593" Symbol="DismissCircle24" Visibility="{Binding FrontendInstalled, Converter={StaticResource InverseBoolToVis}}" />
                            </Grid>
                            <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,4" VerticalAlignment="Center" FontSize="12.5" Foreground="#7c8593" Text="{x:Static res:Strings.Plugin_Row_Frontend}" />
                            <TextBlock Grid.Row="1" Grid.Column="2" Margin="0,4" VerticalAlignment="Center" FontSize="12.5" Foreground="#e5e7eb" Text="{Binding FrontendStatus}" />

                            <!-- Loader (winmm.dll) -->
                            <Grid Grid.Row="2" Grid.Column="0" Margin="0,4" VerticalAlignment="Center">
                                <ui:SymbolIcon FontSize="16" Foreground="#4ade80" Symbol="CheckmarkCircle24" Visibility="{Binding DllOk, Converter={StaticResource BoolToVis}}" />
                                <ui:SymbolIcon FontSize="16" Foreground="#fbbf24" Symbol="Warning24" Visibility="{Binding DllOutOfDate, Converter={StaticResource BoolToVis}}" />
                                <ui:SymbolIcon FontSize="16" Foreground="#7c8593" Symbol="DismissCircle24" Visibility="{Binding DllNotInstalled, Converter={StaticResource BoolToVis}}" />
                            </Grid>
                            <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,4" VerticalAlignment="Center" FontSize="12.5" Foreground="#7c8593" Text="{x:Static res:Strings.Plugin_Row_Loader}" />
                            <TextBlock Grid.Row="2" Grid.Column="2" Margin="0,4" VerticalAlignment="Center" FontSize="12.5" Foreground="#e5e7eb" Text="{Binding DllStatus}" />
                        </Grid>

                        <!-- Millennium coexisting info -->
                        <Border
                            Margin="0,14,0,0"
                            Padding="10,8"
                            Background="#141a78bf"
                            CornerRadius="6"
                            Visibility="{Binding MillenniumCoexisting, Converter={StaticResource BoolToVis}}">
                            <TextBlock
                                FontSize="11.5"
                                Foreground="#93c5fd"
                                Text="{x:Static res:Strings.Plugin_Millennium_Coexisting}"
                                TextWrapping="Wrap" />
                        </Border>

                        <!-- Buttons -->
                        <StackPanel Margin="0,16,0,0" Orientation="Horizontal">
                            <ui:Button
                                MinWidth="120"
                                HorizontalContentAlignment="Center"
                                Command="{Binding InstallCommand}"
                                Content="{Binding InstallButtonText}"
                                IsEnabled="{Binding NotBusy}">
                                <!-- Primary (green) only when there's something to do (Install/Update);
                                     a healthy "Reinstall" stays secondary. -->
                                <ui:Button.Style>
                                    <Style BasedOn="{StaticResource {x:Type ui:Button}}" TargetType="ui:Button">
                                        <Setter Property="Appearance" Value="Secondary" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding InstallIsPrimary}" Value="True">
                                                <Setter Property="Appearance" Value="Primary" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ui:Button.Style>
                            </ui:Button>
                            <ui:Button
                                Margin="10,0,0,0"
                                MinWidth="110"
                                HorizontalContentAlignment="Center"
                                Command="{Binding UninstallCommand}"
                                Content="{x:Static res:Strings.Plugin_Btn_Uninstall}"
                                IsEnabled="{Binding CanUninstall}"
                                Visibility="{Binding IsInstalled, Converter={StaticResource BoolToVis}}" />
                            <ui:Button
                                Margin="10,0,0,0"
                                HorizontalContentAlignment="Center"
                                Command="{Binding CheckForUpdatesCommand}"
                                Content="{x:Static res:Strings.Plugin_Btn_CheckForUpdates}"
                                IsEnabled="{Binding NotBusy}" />
                        </StackPanel>

                        <!-- Progress -->
                        <ProgressBar
                            Height="6"
                            Margin="0,14,0,0"
                            IsIndeterminate="{Binding IsProgressIndeterminate}"
                            Maximum="100"
                            Value="{Binding Progress}"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />

                        <!-- Status line (offline/errors) -->
                        <TextBlock
                            Margin="0,10,0,0"
                            FontSize="12"
                            Foreground="#f59e0b"
                            Text="{Binding StatusLine}"
                            TextWrapping="Wrap"
                            Visibility="{Binding StatusLine, Converter={StaticResource NullToCollapsed}}" />

                    </StackPanel>
                </Border>

                <!-- "What this does". Fills the page and explains the plugin at a glance. -->
                <TextBlock
                    Margin="2,26,0,0"
                    FontSize="11"
                    FontWeight="SemiBold"
                    Foreground="#6b7280"
                    Text="{x:Static res:Strings.Plugin_About_Header}" />

                <StackPanel Margin="0,10,0,0">
                    <StackPanel.Resources>
                        <Style x:Key="FeatureRow" TargetType="Border">
                            <Setter Property="Margin" Value="0,0,0,8" />
                            <Setter Property="Padding" Value="14,12" />
                            <Setter Property="Background" Value="#0DFFFFFF" />
                            <Setter Property="BorderBrush" Value="#1FFFFFFF" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="CornerRadius" Value="10" />
                        </Style>
                    </StackPanel.Resources>

                    <Border Style="{StaticResource FeatureRow}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="34" Height="34" VerticalAlignment="Center" Background="#1aa78bfa" CornerRadius="8">
                                <ui:SymbolIcon FontSize="18" Foreground="#a78bfa" Symbol="AddCircle24" />
                            </Border>
                            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="12.5" Foreground="#c9ced6" Text="{x:Static res:Strings.Plugin_Feature_StoreButton}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FeatureRow}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="34" Height="34" VerticalAlignment="Center" Background="#1aa78bfa" CornerRadius="8">
                                <ui:SymbolIcon FontSize="18" Foreground="#a78bfa" Symbol="ArrowSync24" />
                            </Border>
                            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="12.5" Foreground="#c9ced6" Text="{x:Static res:Strings.Plugin_Feature_AutoUpdate}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource FeatureRow}">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="34" Height="34" VerticalAlignment="Center" Background="#1aa78bfa" CornerRadius="8">
                                <ui:SymbolIcon FontSize="18" Foreground="#a78bfa" Symbol="PuzzlePiece24" />
                            </Border>
                            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" FontSize="12.5" Foreground="#c9ced6" Text="{x:Static res:Strings.Plugin_Feature_Millennium}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <TextBlock
                    Margin="2,10,0,0"
                    FontSize="11.5"
                    Foreground="#6b7280"
                    Text="{x:Static res:Strings.Plugin_Footer}"
                    TextWrapping="Wrap" />

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
